services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: probability-backend
    env_file:
      - .env
      - .env.${PROBABILITY_ENV:-development}
    environment:
      HOST: 0.0.0.0
      PORT: 5000
      UVICORN_RELOAD: "false"
      XIAOGUGIT_API_KEY: ${XIAOGUGIT_API_KEY:-${XG_SERVICE_API_KEY:-${GATEWAY_SERVICE_API_KEY:-}}}
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: probability-frontend
    depends_on:
      - backend
    ports:
      - "3000:3000"
    restart: unless-stopped
